[tox]
envlist = ci27,ci36,ci37,cov-report


[testenv]
setenv =
    COVERAGE_FILE = .coverage.{envname}

[testenv:ci27]
basepython = python2.7
deps =
    -r requirements.txt
commands =
    flake8 Python3.x/
    pytest --doctest-modules --ignore-glob="*__py3.py" Python3.x/
    pytest -s -vv --cov={envsitepackagesdir}/Python3.x --log-level=INFO tests/

[testenv:ci36]
basepython = python3.6
deps =
    -r requirements.txt
commands =
    flake8 Python3.x/
    pytest --doctest-modules --ignore-glob="*__py2.py" Python3.x/
    pytest -s -vv --cov={envsitepackagesdir}/Python3.x --log-level=INFO tests/

[testenv:ci37]
basepython = python3.7
deps =
    -r requirements.txt
commands =
    flake8 Python3.x/
    pytest --doctest-modules --ignore-glob="*__py2.py" Python3.x/
    pytest -s -vv --cov={envsitepackagesdir}/Python3.x --log-level=INFO tests/

[testenv:cov-report]
setenv =
    COVERAGE_FILE = .coverage
deps = coverage
commands =
    coverage combine
    coverage report